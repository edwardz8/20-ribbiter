
echo md5( strtolower( trim( "MyEmailAddress@example.com " ) ) );

md5(email)

const url = `https://www.gravatar.com/avatar/${hash}?d=retro&s=200`;

Vue.component('child', {

  props: ['email'],

  template: '<span>{{ message }}</span>'
})
